import { FilterByCategoryPipePipe } from './filter-by-category.pipe.pipe';

describe('FilterByCategoryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCategoryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
